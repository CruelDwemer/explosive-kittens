import { FC } from 'react'

interface LobbyCanvasProps {
  className?: string
}

const LobbyCanvas: FC<LobbyCanvasProps> = ({ className }) => {
  return <div>lobby-canvas</div>
}

export default LobbyCanvas
